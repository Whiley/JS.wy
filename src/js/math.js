function js$math$abs(x) {
    return Math.abs(x);
}

function js$math$sin(angle,magnitude) {
    return Math.floor(Math.sin(angle) * magnitude);
}

function js$math$cos(angle,magnitude) {
    return Math.floor(Math.cos(angle) * magnitude);
}
